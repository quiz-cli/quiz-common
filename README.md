# quiz-common

Shared dataclass-based quiz models (`Quiz`, `Question`, `Option`) used by quiz components.

## Development

Format and lint:

```bash
$ uv run ruff format
```

```bash
$ uv run ruff check
```
